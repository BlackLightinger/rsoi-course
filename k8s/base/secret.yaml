apiVersion: v1
kind: Secret
metadata:
  name: flight-platform-secrets
type: Opaque
stringData:
  # Development placeholders. CI/CD must replace these before a real deployment.
  GATEWAY_CLIENT_SECRET: replace-me-gateway-secret
  ADMIN_PASSWORD: replace-me-admin-password
  PARTNER_API_KEY: replace-me-partner-key

